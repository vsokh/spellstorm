import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vsokh.spellstorm',
  appName: 'Spellstorm',
  webDir: 'dist',
  backgroundColor: '#07050e',
};

export default config;
